{
  "name": "fund-monitor-feedback",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
